meta {
  name: Get All Distances
  type: http
  seq: 2
}

get {
  url: http://localhost:5000/api/distances
  auth: none
}

tests {
  test("status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("response is array", function() {
    expect(res.getBody()).to.be.an("array");
  });
}
